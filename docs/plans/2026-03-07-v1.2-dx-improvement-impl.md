# v1.2 DX改善 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** OSS公開に向けて、フェーズ遷移のゲート統合、denyガイダンス強化、planコマンド簡素化、init自動検出の4つのDX改善を実装する

**Architecture:** 既存の phase-manager, pre-tool-use hook, plan コマンド, init コマンドに対する小さな改善。全て独立した変更で破壊的変更なし。

**Tech Stack:** TypeScript, node:test, Commander.js, chalk

---

### Task 1: ゲートチェック統合 — テスト追加

`oops phase green` 実行時にゲートチェックが自動実行され、失敗時にはゲート失敗理由を含むエラーが表示されることをテストする。

**Files:**
- Modify: `src/__tests__/commands.test.ts`

**Step 1: テスト追加**

`src/__tests__/commands.test.ts` の末尾に以下を追加:

```typescript
describe('setPhase with gate integration', () => {
  before(() => {
    mkdirSync(OOPS_DIR, { recursive: true });
    writeFileSync(CONFIG_FILE, JSON.stringify(DEFAULT_CONFIG, null, 2));
  });

  after(() => {
    writeTestState({});
  });

  it('should show gate failure reason when gate check fails', async () => {
    writeTestState({ phase: 'RED', featureName: 'gate-test' });
    const { setPhase } = await import('../commands/phase.js');
    // RED→GREEN gate requires failing tests; with no test files, gate should fail
    // setPhase throws on gate failure — verify it throws with gate info
    assert.throws(() => setPhase('green', {}), /Gate check failed/);
  });

  it('should allow transition when --skip-gate is used', async () => {
    writeTestState({ phase: 'RED', featureName: 'gate-test' });
    const { setPhase } = await import('../commands/phase.js');
    assert.doesNotThrow(() => setPhase('green', { skipGate: true }));
    const state = readTestState();
    assert.equal(state.phase, 'GREEN');
  });
});
```

**Step 2: テスト実行で失敗を確認**

Run: `npm test 2>&1 | tail -20`
Expected: 新テスト2件。「should show gate failure reason」は、現在のエラーメッセージが `Gate check failed:` で始まるかどうかで判定。既存の `transitionPhase` が既にゲートを実行するので、このテストは通る可能性がある。通った場合は次のステップの実装は不要（既に統合済み）。失敗した場合はStep 3で修正。

**Step 3: テストが通ることを確認し、コミット**

Run: `npm test`
Expected: 全テストパス

```bash
git add src/__tests__/commands.test.ts
git commit -m "test: ゲートチェック統合のテスト追加"
```

**Note:** 既存の `phase-manager.ts:42-48` で `autoGateCheck` が有効なら既にゲートチェックは統合されている。テストで動作確認後、ゲート失敗時のエラーメッセージ表示を `phase.ts` の `setPhase()` で改善するのが実質的な作業。

---

### Task 2: ゲートチェック統合 — エラー表示改善

ゲートチェック失敗時に、ゲートの `details` と `testOutput` も表示する。

**Files:**
- Modify: `src/commands/phase.ts:26-48`

**Step 1: `setPhase` でゲート失敗の詳細表示を追加**

`src/commands/phase.ts` の `setPhase` 関数を以下のように修正:

```typescript
import { getCurrentPhase, transitionPhase, parsePhase } from '../core/phase-manager.js';
import { readConfig } from '../core/config-manager.js';
import { checkGate } from '../core/gate-checker.js';
import type { Phase } from '../types.js';
import { VALID_TRANSITIONS } from '../types.js';

// ... PHASE_COLORS, PHASE_EMOJI は変更なし ...

export function setPhase(target: string, options: { force?: boolean; skipGate?: boolean }): void {
  const to = parsePhase(target);
  const from = getCurrentPhase();

  // Show gate check details before transition attempt
  const config = readConfig();
  const shouldGate = config.features.autoGateCheck && !options.force && !options.skipGate;
  const GATED: [Phase, Phase][] = [['RED', 'GREEN'], ['GREEN', 'REFACTOR'], ['REFACTOR', 'RED']];
  const isGated = GATED.some(([f, t]) => f === from && t === to);

  if (shouldGate && isGated) {
    const result = checkGate(from, to);
    if (!result.passed) {
      console.log(chalk.red(`\n  ❌ Gate check failed: ${from} → ${to}`));
      console.log(chalk.red(`     ${result.reason}`));
      if (result.details) {
        for (const d of result.details) {
          console.log(chalk.gray(`     ${d}`));
        }
      }
      if (result.testOutput) {
        console.log(chalk.gray(`\n     Test output:\n     ${result.testOutput.split('\n').join('\n     ')}`));
      }
      console.log(chalk.gray(`\n  Use --skip-gate to bypass.`));
      return; // Don't throw — just show error and return
    }
    console.log(chalk.green(`  ✓ Gate check passed`));
  }

  transitionPhase(to, { ...options, skipGate: true }); // Skip gate in transitionPhase since we already checked
  const fromColor = PHASE_COLORS[from];
  const toColor = PHASE_COLORS[to];
  const emoji = PHASE_EMOJI[to];
  console.log(`${emoji} Phase: ${fromColor(from)} -> ${toColor(to)}`);

  // ... 既存のヒントメッセージはそのまま ...
}
```

**Step 2: テスト実行**

Run: `npm test`
Expected: 全テストパス

**Step 3: リント＆コミット**

Run: `npm run lint`

```bash
git add src/commands/phase.ts
git commit -m "feat: ゲートチェック失敗時の詳細表示"
```

---

### Task 3: denyガイダンス強化

PreToolUseフックのdenyメッセージに、フェーズ別の具体的な次ステップを追加する。

**Files:**
- Modify: `src/hooks/pre-tool-use.ts:110-140`

**Step 1: denyメッセージにガイダンスを追加**

`src/hooks/pre-tool-use.ts` の phase rules switch文を修正:

```typescript
  // Phase rules
  switch (phase) {
    case 'RED':
      if (testFile) {
        allow(`Phase: RED - Test file allowed: ${filePath}`);
      } else {
        incrementOops('Attempted to modify implementation during RED phase', filePath);
        process.stderr.write(
          `\n   現在: RED phase（テストファイルのみ編集可）\n` +
          `   次のステップ:\n` +
          `     1. テストファイルを作成/編集して失敗するテストを書く\n` +
          `     2. テストが失敗することを確認\n` +
          `     3. oops phase green でGREENフェーズへ移行\n`,
        );
        deny(`🚫 Phase: RED - Only test files allowed. Cannot modify: ${filePath}`);
      }
      break;

    case 'GREEN':
      if (!testFile) {
        allow(`Phase: GREEN - Implementation file allowed: ${filePath}`);
      } else {
        incrementOops('Attempted to modify tests during GREEN phase', filePath);
        process.stderr.write(
          `\n   現在: GREEN phase（実装ファイルのみ編集可）\n` +
          `   次のステップ:\n` +
          `     1. テストが通る最小限の実装を書く\n` +
          `     2. テストが全て通ることを確認\n` +
          `     3. oops phase refactor でREFACTORフェーズへ移行\n`,
        );
        deny(`🚫 Phase: GREEN - Only implementation files allowed. Cannot modify: ${filePath}`);
      }
      break;

    // REFACTOR case は変更なし
  }
```

**Step 2: テスト実行**

Run: `npm test`
Expected: 全テストパス（deny判定ロジックは変更なし）

**Step 3: リント＆コミット**

Run: `npm run lint`

```bash
git add src/hooks/pre-tool-use.ts
git commit -m "feat: deny時のフェーズ別ガイダンス表示"
```

---

### Task 4: planコマンド簡素化 — createのsubtaskオプショナル化

`oops plan create --goal "..."` でサブタスクなしのプラン作成を可能にする。

**Files:**
- Modify: `src/commands/plan.ts:35-53`
- Modify: `src/cli/index.ts` (plan create コマンド定義)

**Step 1: テスト追加**

`src/__tests__/commands.test.ts` の末尾に追加:

```typescript
describe('createPlanCommand', () => {
  before(() => {
    mkdirSync(OOPS_DIR, { recursive: true });
    writeFileSync(CONFIG_FILE, JSON.stringify(DEFAULT_CONFIG, null, 2));
    writeTestState({});
  });

  after(() => {
    if (existsSync(PLAN_FILE)) rmSync(PLAN_FILE);
  });

  it('should create plan without subtasks', async () => {
    if (existsSync(PLAN_FILE)) rmSync(PLAN_FILE);
    const { createPlanCommand } = await import('../commands/plan.js');
    assert.doesNotThrow(() => createPlanCommand('テスト目標', []));
    const plan = JSON.parse(readFileSync(PLAN_FILE, 'utf-8'));
    assert.equal(plan.goal, 'テスト目標');
    assert.equal(plan.subtasks.length, 0);
    rmSync(PLAN_FILE);
  });
});
```

**Step 2: テスト実行で失敗を確認**

Run: `npm test 2>&1 | grep -A5 "should create plan without subtasks"`
Expected: FAIL — 現在は `At least one subtask is required.` エラーが投げられる

**Step 3: `createPlanCommand` の修正**

`src/commands/plan.ts` の `createPlanCommand` 関数から subtask必須チェックを削除:

```typescript
export function createPlanCommand(goal: string, subtaskArgs: string[]): void {
  if (planExists()) {
    throw new CliError('A plan already exists. Run `oops plan complete` or delete it first.');
  }

  // subtaskArgs が空でもOK — 後から oops plan add で追加
  const subtaskDefs = subtaskArgs.map(parseSubtaskArg);
  const plan = createPlan(goal, subtaskDefs);

  console.log(chalk.bold(`\n📋 Plan created: ${chalk.cyan(goal)}`));
  if (plan.subtasks.length > 0) {
    console.log(`   ${plan.subtasks.length} subtask(s):\n`);
    for (const st of plan.subtasks) {
      console.log(`   ${STATUS_ICONS[st.status]} ${st.id}. ${chalk.bold(st.name)}${st.description ? ` - ${st.description}` : ''}`);
    }
  } else {
    console.log(chalk.gray('   No subtasks yet. Add with: oops plan add "name: description"'));
  }
  console.log('');
}
```

**Step 4: テスト実行で通過を確認**

Run: `npm test`
Expected: 全テストパス

**Step 5: コミット**

```bash
git add src/commands/plan.ts src/__tests__/commands.test.ts
git commit -m "feat: plan createのsubtaskオプショナル化"
```

---

### Task 5: planコマンド簡素化 — plan addの引数簡素化

`oops plan add "name: description"` で `--subtask` フラグ不要にする。

**Files:**
- Modify: `src/cli/index.ts` (plan add コマンド定義)

**Step 1: `plan add` コマンドの引数定義変更**

`src/cli/index.ts` の plan add セクションを修正:

```typescript
plan
  .command('add [subtask]')
  .description('Add a subtask to the existing plan')
  .option('--subtask <subtask>', 'Subtask in "name: description" format (deprecated, use positional arg)')
  .action((subtask, options) => {
    const arg = subtask ?? options.subtask;
    if (!arg) {
      throw new CliError('Subtask argument required. Usage: oops plan add "name: description"');
    }
    addSubtaskCommand(arg);
  });
```

**Step 2: テスト実行**

Run: `npm test`
Expected: 全テストパス（addSubtaskCommand の中身は変更なし）

**Step 3: リント＆コミット**

Run: `npm run lint`

```bash
git add src/cli/index.ts
git commit -m "feat: plan addの位置引数対応"
```

---

### Task 6: init テストコマンド自動検出 — テスト追加

プロジェクトタイプ別のテストコマンド自動検出関数のテストを先に書く。

**Files:**
- Create: `src/__tests__/init.test.ts`

**Step 1: テスト作成**

```typescript
import { describe, it, before, after, beforeEach } from 'node:test';
import assert from 'node:assert/strict';
import { writeFileSync, mkdirSync, rmSync, existsSync } from 'node:fs';
import { join } from 'node:path';

// Use a temp directory to avoid modifying real project files
const TMP_DIR = '.oops-init-test-tmp';

describe('detectTestCommand', () => {
  before(() => {
    mkdirSync(TMP_DIR, { recursive: true });
  });

  after(() => {
    rmSync(TMP_DIR, { recursive: true, force: true });
  });

  beforeEach(() => {
    // Clean tmp dir contents
    for (const f of ['package.json', 'pyproject.toml', 'pytest.ini', 'Cargo.toml', 'go.mod', 'Makefile']) {
      const p = join(TMP_DIR, f);
      if (existsSync(p)) rmSync(p);
    }
  });

  it('should detect npm test for Node.js projects', async () => {
    writeFileSync(join(TMP_DIR, 'package.json'), '{}');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'npm test');
  });

  it('should detect pytest for Python projects', async () => {
    writeFileSync(join(TMP_DIR, 'pyproject.toml'), '');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'pytest');
  });

  it('should detect pytest for pytest.ini projects', async () => {
    writeFileSync(join(TMP_DIR, 'pytest.ini'), '');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'pytest');
  });

  it('should detect cargo test for Rust projects', async () => {
    writeFileSync(join(TMP_DIR, 'Cargo.toml'), '');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'cargo test');
  });

  it('should detect go test for Go projects', async () => {
    writeFileSync(join(TMP_DIR, 'go.mod'), '');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'go test ./...');
  });

  it('should detect make test for Makefile projects', async () => {
    writeFileSync(join(TMP_DIR, 'Makefile'), 'test:\n\techo "testing"');
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), 'make test');
  });

  it('should return null when no project file detected', async () => {
    const { detectTestCommand } = await import('../commands/init.js');
    assert.equal(detectTestCommand(TMP_DIR), null);
  });
});
```

**Step 2: テスト実行で失敗を確認**

Run: `npm test 2>&1 | grep -A5 "detectTestCommand"`
Expected: FAIL — `detectTestCommand` がまだ存在しない

**Step 3: コミット（失敗テスト）**

テストが失敗することを確認してから、次のTaskで実装する。この時点ではコミットしない（Task 7と合わせてコミット）。

---

### Task 7: init テストコマンド自動検出 — 実装

**Files:**
- Modify: `src/commands/init.ts`

**Step 1: `detectTestCommand` 関数を追加**

`src/commands/init.ts` の `// --- Helper functions ---` セクションの前に追加:

```typescript
// --- Test command detection ---

const PROJECT_DETECTORS: { file: string; command: string; check?: (dir: string) => boolean }[] = [
  { file: 'package.json', command: 'npm test' },
  { file: 'pyproject.toml', command: 'pytest' },
  { file: 'pytest.ini', command: 'pytest' },
  { file: 'Cargo.toml', command: 'cargo test' },
  { file: 'go.mod', command: 'go test ./...' },
  {
    file: 'Makefile',
    command: 'make test',
    check: (dir: string) => {
      try {
        const content = readFileSync(join(dir, 'Makefile'), 'utf-8');
        return /^test\s*:/m.test(content);
      } catch {
        return false;
      }
    },
  },
];

export function detectTestCommand(dir: string = '.'): string | null {
  for (const detector of PROJECT_DETECTORS) {
    if (existsSync(join(dir, detector.file))) {
      if (detector.check && !detector.check(dir)) continue;
      return detector.command;
    }
  }
  return null;
}
```

**Step 2: `initOops` に自動検出を統合**

`initOops` の関数シグネチャと config 作成部分を修正:

```typescript
export function initOops(options: { force?: boolean; testCommand?: string } = {}): void {
  const force = options.force ?? false;

  // ... 既存のヘッダー出力 ...

  // 1. .oops/ directory + state + config
  // ... state の部分は変更なし ...

  if (!configExists() || force) {
    const detected = options.testCommand ?? detectTestCommand();
    const config = { ...DEFAULT_CONFIG };
    if (detected) {
      config.testCommand = detected;
    }
    writeConfig(config);
    console.log(chalk.green('  ✅ Created .oops/config.json'));
    if (detected && detected !== DEFAULT_CONFIG.testCommand) {
      console.log(chalk.cyan(`     Test command auto-detected: ${detected}`));
    }
  } else {
    console.log(chalk.gray('  ✓ .oops/config.json exists'));
  }

  // ... 残りは変更なし ...
}
```

**Step 3: CLIに `--test-command` オプション追加**

`src/cli/index.ts` の init コマンド定義を修正:

```typescript
program
  .command('init')
  .description('Initialize OOPS Framework in current project')
  .option('--force', 'Overwrite existing files')
  .option('--test-command <command>', 'Override test command (default: auto-detect)')
  .action((options) => initOops(options));
```

**Step 4: テスト実行**

Run: `npm test`
Expected: 全テストパス

**Step 5: リント＆コミット**

Run: `npm run lint`

```bash
git add src/commands/init.ts src/cli/index.ts src/__tests__/init.test.ts
git commit -m "feat: oops initのテストコマンド自動検出"
```

---

### Task 8: バージョン更新・最終検証

**Files:**
- Modify: `package.json`
- Modify: `src/cli/index.ts`

**Step 1: バージョン更新**

`package.json` の `version` を `"1.2.0"` に変更。
`src/cli/index.ts` の `.version('1.1.0')` を `.version('1.2.0')` に変更。

**Step 2: 全テスト実行**

Run: `npm test`
Expected: 全テストパス（115 + 新規テスト）

**Step 3: リント**

Run: `npm run lint`
Expected: エラーゼロ

**Step 4: コミット**

```bash
git add package.json src/cli/index.ts
git commit -m "chore: v1.2.0"
```
